replicaCount: 1

image:
  repository: demo
  pullPolicy: IfNotPresent
  tag: latest

postgresql:
  enabled: true
  postgresqlUsername: tilt
  postgresqlPassword: tilt
  postgresqlDatabase: tilt
  postgresqlPort: 5432